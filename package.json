{
  "name": "extensionsubscription",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "backend/src/**/*.ts": [
      "npm --prefix backend run lint"
    ],
    "extension/src/**/*.{ts,tsx}": [
      "npm --prefix extension run lint"
    ]
  }
}
